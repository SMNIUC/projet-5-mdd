# Server
server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/mdddb
spring.datasource.username=postgres
spring.datasource.password=UN6N1=VH")``r+_ykB4Z
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# Flyway
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# JWT — set JWT_SECRET env var in production to a cryptographically random 256-bit hex string
# e.g.: export JWT_SECRET=$(openssl rand -hex 32)
jwt.secret=${JWT_SECRET:=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.expiration=86400000

# Cookie — set to true when deployed over HTTPS
app.cookie.secure=${COOKIE_SECURE:=false}
